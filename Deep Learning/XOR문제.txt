선으로는 같은 색의 점들을 나눌 수 없다라는 단일 신경망(퍼셉트론)의 한계가 있다.
퍼셉트론의 한계를 설명할 때 XOR을 많이 예로 든다. XOR은 a b 가 같을 때 0, 다를 때 1을 출력한다.
이를 해결할 수 있는 방법은 다중 퍼셉트론이다.


과대적합 오버피팅을 방지하는 방법으로 Dropout이 있다. 이는 해당 레이어에 무작위로 노드를 없애는 것이다.